{
  "name": "ai-task-executor",
  "version": "1.0.0",
  "description": "AI Task Executor project that polls ClickUp for AI tasks, executes them, commits, pushes, and notifies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "fastify": "^4.12.0",
    "node-cron": "^3.0.0",
    "node-fetch": "^2.7.0"
  }
}
